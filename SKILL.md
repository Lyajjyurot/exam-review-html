---
name: exam-review-html
description: 把用户上传的 PDF / Word / 文本考试提纲、复习资料、知识点清单解析并批注，生成单个自包含 HTML 可视化复习页。页面按科目分标签切换，每个科目包含知识要点（含批注与分级）、速背速记（口诀/对比表/公式卡，可遮罩自测）、模拟题（在线作答即时判分、参考答案与逐项解析）、易错点与考前自测清单，支持全局搜索、深浅色切换、进度本地存档与打印。当用户要求「把提纲做成复习网页」「生成期末复习资料/冲刺包」「解析考点并出题」「做可交互的背诵页面」时使用本技能。
agent_created: true
---

# 考试提纲 → 交互式 HTML 复习页

## 用途

把一份或多份 PDF/Word 考试提纲，转换成一个**单文件 HTML** 复习站：一个科目一个顶部标签按钮，切换后依次呈现知识要点、速背速记、模拟题（含参考答案与解析）、易错与自测清单。产出物双击即可用，不联网、不依赖任何 CDN，手机与电脑都能打开。

## 何时使用

- 用户上传 PDF / Word 的考试提纲、复习大纲、知识点汇总、考点清单
- 用户说"帮我做成复习网页""整理成期末复习资料""按这个提纲出题""生成速记页面"
- 一次上传多个文件（每份可能是一门课，或同一门课的上下册）

## 环境

解析脚本需要 Python 3.8+ 及 `pypdf` 与 `python-docx` 依赖。

```bash
pip install pypdf python-docx
```

国内网络可加镜像：

```bash
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple pypdf python-docx
```

`.docx` 在没有任何依赖时也能解析（脚本内置标准库回退），`.pdf` 必须装库。旧版 `.doc` 无法解析，提示用户另存为 `.docx`。

## 工作流

### 第 1 步：确认输入与分科

先读一遍文件清单，判断"几个文件 = 几门课"：
- 文件名/原文标题含科目名 → 直接作为 `subjects[].name`。
- 多个文件明显属于同一科目（如"高数上/下"）→ 合并为一个科目的不同章节，并告知用户。
- 判断不了就按文件切分，并在最后询问是否要合并。

默认规模（用户没特别说明时按此执行）：每科 20–40 个要点、3–6 张速记卡、12–20 道题。文档很长时按"高频优先"取舍，不要为了凑数把边角料全塞进去。

在开工前向用户复述一遍科目划分，避免白做。

### 第 2 步：抽取原文

```bash
<python> scripts/extract_document.py <文件或目录> -o ./extracted --json
```

输出 `./extracted/<文件名>.md`（保留标题层级、表格、分页标记）和 `_manifest.json`（字数/标题数/页数）。
- 提纲超过 60 页时可加 `--max-pages` 先抽样看结构，确认后再全量抽取。
- 抽取结果若是扫描件（几乎无文字），立即告知用户：PDF 是图片版，需要 OCR 或换文本版，不要硬编内容。
- 抽取后**必须通读全文**再进第 3 步，不要只看标题。

### 第 3 步：批注与出题（核心，决定质量）

严格按 `references/pedagogy.md` 执行：
- **要点分级**：core / key / basic，core 控制在 20%–30%。
- **批注（note）**只写易错、条件、关联、考法四类信息，禁止复述正文和"很重要"式空话。
- **速记卡**混合使用口诀 / 对比表 / 公式卡 / 数字归纳 / 流程清单 / 反例卡，不要全是罗列。
- **命题**：按 单选40% / 多选判断20% / 填空名解20% / 简答计算20% 配比；干扰项用偷换条件、张冠李戴、以偏概全、因果倒置、形近混淆五种手法从原文取材；解析按"考点定位 → 逐项说明 → 易错提醒"三段式。
- 每章至少 2 题，每个 core 要点至少被 1 题覆盖，`source` 回指具体章节。

`assets/sample_content.json` 是一份完整可跑的示例（2 个科目），只作为结构参考，**不是内容质量基准**——真实产出的要点数与题量应远大于它。不确定字段写法时先读它，再读 `references/content-model.md`。

字段规范见 `references/content-model.md`，严格按该 JSON 结构写 `review_content.json`（建议放在工作目录下）。

### 第 4 步：构建 HTML

```bash
<python> scripts/build_review_html.py --content review_content.json --out 期末复习.html
```

脚本会校验必填字段、自动补默认色/ID/缺省小节，并打印内容体检报告（每科要点/速记/题目数 + 过瘦告警）。出现告警时回去补内容，不要直接交付。

### 第 5 步：交付与检查

1. 用 present_files 打开生成的 HTML。
2. 检查：科目切换是否齐全、模拟题点选后是否即时判分并显示解析、搜索能否命中、打印预览是否正常。
3. 在回复里给出这份资料的使用顺序建议（先过要点 → 遮罩自测速记 → 限时做题 → 回看易错清单），并说明答题进度会存在本机浏览器。

## 常见修订

- **加科目/加题**：让用户补文件，或直接在 `review_content.json` 里追加后重跑第 4 步（不要重写整个 HTML）。
- **改配色/版式**：编辑 `assets/template.html`，重跑构建脚本即可。科目主色由 JSON 的 `color` 控制，通常无需改模板。
- **只要某一科**：删掉 `subjects` 里其他科目重跑，或让用户自己在页面切标签。
- **答案有争议**：改 `answer` / `analysis` 后重跑构建，比手改 HTML 可靠。

## 约束

- 产出物必须是**单个自包含 HTML**：不得引入外部 CSS/JS/字体/CDN 链接，不得拆成多文件（用户要求单文件内切换）。
- 内容忠于原文：要点正文不得杜撰；批注与解析只做公认常识范围内的补充。
- 不要直接手写成千上万行的 HTML —— 一律走"写 JSON + 跑脚本"的路径，便于后续增量修订。
